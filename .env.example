# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:8000

# Same value as backend LEADS_API_KEY when using production API (DEBUG=False)
# VITE_LEADS_API_KEY=your-long-random-secret
